class Animal {
  especie: string;
  quantidade: number;
  justificativa: string;

  constructor(especie: string, quantidade: number, justificativa: string) {
    this.especie = especie;
    this.quantidade = quantidade;
    this.justificativa = justificativa;
  }
}
